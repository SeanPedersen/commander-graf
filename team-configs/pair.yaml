name: "Pair: Builder + Reviewer"
description: One builds, one reviews
agents:
  - name: builder
    model: sonnet
    runtime: claude-code
    prompt: Implement the feature described in the task.
    agent_type: implementer
  - name: reviewer
    model: sonnet
    runtime: claude-code
    prompt: Review the code changes for quality, security, and correctness.
    agent_type: reviewer
